#include <iostream>
#include <string>
#include <vector>
#include <set>
#include <cmath>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;
using P = pair<int, int>;
long mod = (int) 1e9 + 7;

int main(){
    
    string s;
    cin >> s;
    if(s == "ABC") cout << "ARC";
    if(s == "ARC") cout << "ABC";

    
    return 0;
}

