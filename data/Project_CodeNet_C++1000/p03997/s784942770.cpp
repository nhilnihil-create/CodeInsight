#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
typedef long long ll;
using namespace std;

int main() {
    int a, b, h;
    cin >> a >> b >> h;

    int ans = (a + b) * h / 2;

    cout << ans << endl;
    return 0;
}
