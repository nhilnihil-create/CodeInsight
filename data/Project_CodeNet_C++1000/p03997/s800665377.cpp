#include<bits/stdc++.h>

using namespace std;

typedef long long llint;

int a, b, h;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cin >> a >> b >> h;
    cout << (a + b) * h / 2;
    return 0;
}
