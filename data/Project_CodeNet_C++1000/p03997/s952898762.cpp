#include "bits/stdc++.h"

using namespace std;

void Main() {
    int a, b, h;
    cin >> a >> b >> h;

    cout << (a + b) * h / 2 << endl;
}

int main() {
    std::cout << std::fixed << std::setprecision(15);
    Main();
    return 0;
}
