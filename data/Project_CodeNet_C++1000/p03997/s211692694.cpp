#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef pair<ll, ll> pii;
const ll inf = 1e6+5;
typedef pair<int, bool> pib;
int main() {
    int a,b,h; cin >> a >> b >> h;
    cout << (h*(a+b))/2;
}

