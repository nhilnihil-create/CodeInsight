#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;
const double PI=acos(-1);

int main(){
    int a, b, h;
    cin >> a >> b >> h;

    int S = (a + b) * h / 2;

    cout << S << endl;

    return 0;
    
}