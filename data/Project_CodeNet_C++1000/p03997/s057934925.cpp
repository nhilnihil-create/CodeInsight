#include <bits/stdc++.h>
using namespace std;
#define rep(i, ns, ne) for (int i = ns; i < ne; ++i)
#define rep2(i, ns, ne) for (int i = ns; i <= ne; ++i)

int main() {
  int a, b, h;
  cin >> a >> b >> h;
  cout << ((a + b) * h / 2) << endl;
  getchar();
}