#include <iostream>
#include <string>

using std::cout;
using std::endl;
using std::cin;
using std::string;
int main() {
	string W;
	string T;
	int sum = 0;
	cin >> W;
	// ??\????????§??????????????????
	for (size_t i = 0; i < W.size(); i++)
	{
		W[i] = toupper(W[i]);
	}
	
	while (1)
	{
		cin >> T;
		// ???????????¶
		if (T == "END_OF_TEXT")	break;
		// ??\????????§??????????????????
		for (size_t i = 0; i < T.size(); i++)
		{
			T[i] = toupper(T[i]);
		}

		// ????´¢
		if (W == T) sum++;
	}
	cout << sum << endl;
	return 0;
}