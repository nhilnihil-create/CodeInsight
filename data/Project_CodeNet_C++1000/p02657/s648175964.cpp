#include <bits/stdc++.h> 
using namespace std; 
  
#define rep(i,k,n)  for (int i = k; i < n; ++i)
#define repr(i,k,n) for (int i = k; i >= n; --i)

int main(void) {
  int a,b;
  cin >> a >> b;
  cout << a * b << "\n";
  return 0;
}

