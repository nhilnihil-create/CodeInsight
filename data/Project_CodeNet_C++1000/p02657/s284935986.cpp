#include <bits/stdc++.h>

using namespace std;

int main()
{
    long double a,b;
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> a >> b;
    long long c = a*b;
    cout << c;
    return 0;
}
