#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;
using P = pair<int, int>;

const int M = 1000005;

int main()
{
    int x, y;
    cin >> x >> y;

    cout << x * y << endl;

    return 0;
}