#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    if (a + b >= 10)
    {
        puts("error");
    }
    else
    {
        printf("%d\n", a + b);
    }
}