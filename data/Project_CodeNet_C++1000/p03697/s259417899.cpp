#include <bits/stdc++.h>
using namespace std;
int main(void){

    int a, b;
    cin >> a >> b ;
    
    //*
    if(a + b > 9 ){
        cout << "error" << endl;
    } else {
        cout << a + b << endl;
    }
    //*/
    
    //cout << a.at(0) - 'a' + 'A' << b.at(0) - 'a' + 'A' << c.at(0) - 'a' + 'A' << endl;

    return 0; 
}
