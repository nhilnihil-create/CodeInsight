#include<bits/stdc++.h>
#define ll long long int
#define FAST() ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
using namespace std;
int main()
{
    FAST();
    ll n,i,j,k,a,b;
    cin>>a>>b;
    if(a+b>=10)
        cout<<"error"<<endl;
    else
        cout<<a+b<<endl;

    return 0;
}
