#include <bits/stdc++.h>

using namespace std;

int main(int argc, const char *argv[]) {
  string s;
  cin >> s;
  cout << 2018;
  for (int i = 4; i < s.size(); ++i) {
    cout << s[i];
  }

  cout << '\n';
  return 0;
}