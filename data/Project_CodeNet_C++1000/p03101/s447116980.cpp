#include <bits/stdc++.h>
#define rep(i,n) for(int i=0; i<(n); i++)
using namespace std;
using ll  = long long;
using v   = vector<int>;
using P   = pair<int,int>;
using vp  = vector<P>;
using vvp = vector<vp>;

int main(){
    int n,m,a,b;
    cin>>n>>m>>a>>b;
    cout << (n-a)*(m-b) << endl;
    
    return 0;
}