#include <bits/stdc++.h>
#define rep(i,n) for(int i = 0; i < (n); i++)
typedef long long ll;
using namespace std;

int main(){
    int h,w,a,b;
    cin >> h >> w >> a >> b;
    cout << (h - a) * (w - b) << endl;
}
