#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define REP(i, n) for (int i = 0, i##_len = (n); i < i##_len; ++i)

int main()
{
    int n;
    cin >> n;
    if (n <= 999)
    {
        cout << "ABC"  << "\n";
    }
    else
    {
        cout << "ABD"  << "\n";
    }
    return 0;
}
