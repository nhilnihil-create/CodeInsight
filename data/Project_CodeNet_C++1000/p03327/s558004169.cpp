#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> P;
#define rep(i,n) for (int i = 0; i < (n); ++i)
int n, m;
int main() {
    int n;
    cin >> n;
    if ( n >= 1000 ) cout << "ABD" << endl;
    else             cout << "ABC" << endl;
    return 0;
}