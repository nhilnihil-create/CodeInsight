#include <algorithm>
#include <cmath>
#include <iostream>
#include <set>
using namespace std;
using ll = long long;
#define repl(i, l, r) for (int i = l; i < r; i++)
#define rep(i, n) repl(i, 0, n)
int main() {
	ll n;
	cin >> n;
	ll a[5];
	if (n < 1000)
		cout << "ABC" << endl;
	else
		cout << "ABD" << endl;
	return 0;
}