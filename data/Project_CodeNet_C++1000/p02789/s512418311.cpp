#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N=305;
int n,s,be[N],a[N][N],b[N][N];
char S[N*N];
long double pi=acos(-1);
int main()
{ 
    int n,m;
    cin>>n>>m;
    if(n<=m) puts("Yes");
    else puts("No");
    return 0;
}
