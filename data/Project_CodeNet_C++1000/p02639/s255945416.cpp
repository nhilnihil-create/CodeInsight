#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int,int>;

void solve() {
    rep(i, 5) {
        int x; cin >> x;
        if (x == 0) cout << i + 1 << endl;
    }
}

int main() {
    solve();
    return 0;
}
