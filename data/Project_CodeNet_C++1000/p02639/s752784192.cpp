#include <iostream>
#include <cstring>
#include <vector>
#include <algorithm>
using namespace std;
long long n,i,a;
int main() {
    for(i = 1; i <= 5; i++) {
        cin >> a;
        if(!a) {cout << i; break;}
    }
    return 0;
}