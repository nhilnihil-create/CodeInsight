#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
#define rad_to_deg(rad) (((rad)/2/M_PI)*360)
using ll = long long;
using namespace std;
int main(){
    string s;
    int n,k;
    cin>>n>>k>>s;
    k--;
    s[k]+=32;
    cout<<s<<endl;
}
