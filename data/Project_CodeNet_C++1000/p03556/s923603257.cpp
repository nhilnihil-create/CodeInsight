#include <bits/stdc++.h>
#include <set>
#define rep(i,n) for (int i=0; i < (n); ++i)
#define all(a) a.begin(), a.end()
using ll = long long;
using namespace std;

// long long l[100];

int main() {
  int n;
  cin >> n;
  int ans =sqrt(n);
  cout << ans*ans << endl;
  return 0;
}