#include <bits/stdc++.h>
using namespace std;

int main() {
  
  int a, b;
  cin >> a >> b;
  int p = a - (b * 2);
  if(p < 0) {
    cout << 0 << endl;
  }
  else {
    cout << p << endl;
  }
  
  
  
}