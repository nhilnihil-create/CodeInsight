#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i = 0; i < n; i++)
#define REP(i,s,n) for(int i = s; i < n; i++)
int main(){
  int g,p,s;
  cin >> g >> p >>s;
  cout << (g * p) / 2 << endl;
  return 0;
}