#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (ll i = 0; i < (ll)(n); i++)
typedef long long ll;
//------------------------------------------//

int main(){
  double N;cin>>N;
  int M=(N+1)/2;
  cout<<fixed<<setprecision(7)<<(double)(M/N)<<endl;
}