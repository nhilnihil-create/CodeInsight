#include <bits/stdc++.h>
using namespace std;

void solve(int N)
{
    cout << fixed << setprecision(10);
    cout << (double)((N + 1) / 2) / N << endl;
}

int main()
{
    int N;
    cin >> N;
    solve(N);
    return 0;
}
