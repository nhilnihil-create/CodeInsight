#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
	int n;
	cin>>n;
	// cout.precision(10);
	 cout.precision(10);
	cout<<(double)(n - n/2)/double(n)<<endl; 
	return 0;
}