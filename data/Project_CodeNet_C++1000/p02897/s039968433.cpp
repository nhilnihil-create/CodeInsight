#include <bits/stdc++.h>
#define DAU  ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define PLEC exit(0);
using namespace std;
int n;
int main() {
    DAU
    cin >> n;
    cout << fixed << setprecision(10) << 1.0 - 1.0 * (n / 2) / n;
    PLEC
}
