#include <bits/stdc++.h>
using namespace std;

int main() {
  // ここにプログラムを追記
  int n, a, b, x;
  cin >> n >> a >> b;
  x = n * a;
  if(x > b){
    cout << b << endl;
  }
  else if(x == b){
    cout << x << endl;
  }
  else{
    cout << x << endl;
  }
}