#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;

int main() {
  int X;
  cin >> X;
  string ans = X >= 30 ? "Yes" : "No";
  cout << ans << endl;
  return 0;
}