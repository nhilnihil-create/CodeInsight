#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
using ll = long long int;

void solve(int &T)
{
    ll n;
    cin >> n;
    cout << (n >= 30 ? "Yes" : "No");
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    int t = 1;
    for(int i = 1; i <= t; i++) {
        solve(i);
    }
    return 0;
}