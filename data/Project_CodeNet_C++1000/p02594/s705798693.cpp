#include <bits/stdc++.h>
#define mem(a,b) memset(a,b,sizeof a)
#define MAX 100005
using namespace std;
typedef long long ll;
int main()
{
    ios::sync_with_stdio(false);
    int t;
    cin>>t;
    if(t>=30)
        cout<<"Yes";
    else
        cout<<"No";
    return 0;
}