#include <bits/stdc++.h>
using ll = long long int;
using namespace std;

int main()
{
    pair<int, int> A, B;
    cin >> A.first >> A.second >> B.first >> B.second;
    if (A.first == B.first)
    {
        cout << 0 << endl;
    }
    else
    {
        cout << 1 << endl;
    }

    return 0;
}