#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
using Graph = vector<vector<int>>;
#define rep(i,n) for(int i=0;i<(int)(n);++i)

int main() {
  int a,b,c,d;
  cin>>a>>b>>c>>d;
  if(d==1) cout<<"1"<<endl;
  else cout<<"0"<<endl;
}
