#include <bits/stdc++.h>
#define rep(i,x,n) for(int i=x; i<(int)(n); i++)
#define rep_eq(i,x,n) for(int i=x; i<=(int)(n); i++)
using namespace std;

using ll=long long;

int main() {
  int M1,D1; cin >>M1 >>D1;
  int M2,D2; cin >>M2 >>D2;
  cout <<(M1!=M2 ? 1:0) <<endl;
  return 0;
}
