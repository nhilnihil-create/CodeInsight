#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define rep(i,x) for(int i=0;i<x;i++)
#define put(ans)    cout << ans << endl;

int main(){
    int a,b,c,d;
    cin >> a >> b >> c >> d;

    if(a!=c){
        cout << 1 << endl;
    }else
        cout << 0 << endl;

    return 0;
}