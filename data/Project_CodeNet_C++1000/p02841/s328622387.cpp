#include <bits/stdc++.h>
#define rep(i, n) for (ll i = 0; i < n; ++i)
typedef long long ll;
using namespace std;
const int INF = 1e9;

int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;

    if (b > d)
        cout << "1";
    else
        cout << "0";
    cout << endl;

    return 0;
}
