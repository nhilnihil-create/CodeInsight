#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    int a, b;
    cin >> a >> b ;
    //*/

    /*
    vector<int> vec(4);
    cin >> vec.at(0) >> vec.at(1) >> vec.at(2) ;
    
    sort(vec.begin(), vec.end());
    */
    
    //*
    if( a <= b){
        cout << a << endl;
    } else {
        cout << a - 1 << endl;
    }
    //*/
    
    //cout << min(a, b) << endl;

    return 0; 
}
