#include <bits/stdc++.h>
#define rep(i,m,n) for(int i=m; i<n; i++)
#define co(n) cout << n << endl
using namespace std;
int main(){
  string s;
  cin >> s;
  char a[s.size()];
  int c=0, d=0;
  rep(i,0,s.size()-8) cout << s.at(i);
  cout << endl;
  
  
  return 0;
}
