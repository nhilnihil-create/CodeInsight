#include <bits/stdc++.h>
#define INF 1e9
using namespace std;

signed main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    string s;
    cin>>s;
    for(int i=0;i<s.size()-8;i++)cout<<s[i];
    cout<<endl;
}