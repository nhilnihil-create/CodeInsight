#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main() {
        ios_base::sync_with_stdio(0);
        cin.tie(0);
        cout.tie(0);

        int x;

        cin >> x;

        if (x == 5 || x == 7 || x == 3) {
                cout << "YES" << endl;
        } else {
                cout << "NO" << endl;
        }

        return 0;
}