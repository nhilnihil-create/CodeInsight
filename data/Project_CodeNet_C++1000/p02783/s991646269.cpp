#include <bits/stdc++.h>
using namespace std;
using ll = long long;

signed main()
{
  cin.tie(0);
  ios::sync_with_stdio(false);

  int h, a;
  cin >> h >> a;

  cout << (h + a - 1) / a << '\n';

  return (0);
}
