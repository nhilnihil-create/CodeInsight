#include <iostream>
using namespace std;

int main( void )
{
    int a, b, c;
    cin >> a >> b >> c;
    if ( ( 2 * b ) == ( a + c ) )
    {
        cout << "YES";
    }
    else
    {
        cout << "NO";
    }

    return 0;
}
