#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);

    int a,b,c;
    cin >> a >> b >> c;
    if(b-a==c-b) puts("YES");
    else puts("NO");

    return 0;
}