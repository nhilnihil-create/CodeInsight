#include <bits/stdc++.h>
using namespace std;
using ll = int64_t;
#define rep(i,n) for(int i = 0; i < ((int)(n)); i++)   // 0-indexed昇順

int main() {
    int i = 0;
    string s;
    cin >> s;
    cout << s.at(i++);
    cin >> s;
    cout << s.at(i++);
    cin >> s;
    cout << s.at(i++) << endl;
}
