#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int,int>;

int main(){
    string s0, s1, s2;
    cin >> s0 >> s1 >> s2;
    cout << s0.at(0) << s1.at(1) << s2.at(2) << endl;
    return 0;
}