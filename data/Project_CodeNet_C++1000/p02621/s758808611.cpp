#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;


int main()
{
  int a;
  cin >> a;
  printf("%d\n", a + a * a + a * a * a);
  return 0;
}