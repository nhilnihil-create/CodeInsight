#include<bits/stdc++.h>
using namespace std;
#define faster() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define endl '\n'

int main()
{
    faster();


    int a;
    cin>>a;
    int ans;
    ans = a + (a*a) + (a*a*a);
    cout<<ans<<endl;

	return 0;
}
