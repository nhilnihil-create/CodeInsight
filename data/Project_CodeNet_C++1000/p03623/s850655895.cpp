#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    int a, b, c;
    cin >> a >> b >> c;
    //*/
    
    /*
    vector<int> a(3);
    cin >> a.at(0) >> a.at(1) >> a.at(2) ;
    
    sort(a.begin(), a.end());
    */
    
    //*
    if( abs(b - a) <= abs(c - a) ){
        cout << "A" << endl;
    } else {
        cout << "B" << endl;
    }
    //*/
    
    //cout << ( a - 1 ) * ( b - 1 ) << endl;

    return 0; 
}
