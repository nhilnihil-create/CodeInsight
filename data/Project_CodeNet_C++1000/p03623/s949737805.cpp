#include <bits/stdc++.h>
using namespace std;
map<long,long>mp;
int main()
{
    long i,j,t,a,b,n,c,y,m,prev,sum,ans=1;
    t=1;
    // cin>>t;
    while(t--)
    {
       string s;
       cin>>n>>a>>b;
       if(abs(n-a)>abs(n-b))cout<<"B";
       else cout<<"A";
    }
}