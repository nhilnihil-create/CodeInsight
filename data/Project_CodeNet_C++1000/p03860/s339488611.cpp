#include <bits/stdc++.h>
using namespace std;

int main() {
    string first, second, third;
    
    cin >> first >> second >> third;
    
    cout << first.at(0) << second.at(0) << third.at(0) << endl;
}