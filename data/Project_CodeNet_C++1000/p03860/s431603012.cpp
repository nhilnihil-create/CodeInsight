#define rep(i, n) for (int i = 0; i < (n); i++)
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
    string A, s, C;
    cin >> A >> s >> C;

    cout << A.at(0) << s.at(0) << C.at(0) << endl;

    return 0;
}