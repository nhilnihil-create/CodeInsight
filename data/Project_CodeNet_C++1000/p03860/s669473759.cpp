#include <bits/stdc++.h>
using namespace std;

int main() {
 string X, Y, Z;
 cin >> X >> Y >> Z;
  for (int i = 0; i < X.size(); i++) {
    cout << X.at(0);
	break;   
  }
  for (int i = 0; i < Y.size(); i++) {
    cout << Y.at(0);
    break;
  }
  for (int i = 0; i < Z.size(); i++) {
    cout << Z.at(0);
    break;
  }
}  


