#include <bits/stdc++.h>
using namespace std;
using ll = long long;

signed main()
{
  cin.tie(0);
  ios::sync_with_stdio(false);

  string a, b, c, ans;
  cin >> a >> b >> c;

  ans += a[0];
  ans += b[0];
  ans += c[0];

  cout << ans << endl;

  return (0);
}
