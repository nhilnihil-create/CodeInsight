#include <bits/stdc++.h>
using namespace std;

int main() {
    string _s, s;
    for (int i = 0; i < 3; i++) {
        if (i == 1) cin >> s;
        else cin >> _s;
    }
    cout << 'A' << s.at(0) << 'C' << endl;
}
