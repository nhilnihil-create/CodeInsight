#include<bits/stdc++.h>
using namespace std;
 
#define ll long long

void shakespeare() {
  string a,b,c;
  cin >> a >> b >> c;
  cout << a.at(0) << b.at(0) << c.at(0) << endl;
}

signed main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int tt=1;
  //cin >> tt;
  while(tt--) shakespeare();
 
  return 0;
}