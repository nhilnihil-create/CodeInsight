#define rep(i, n) for (int i = 0; i < (n); i++)
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
    string A, s, C;
    cin >> A >> s >> C;

    char B = s.at(0);

    cout << "A" << B << "C" << endl;

    return 0;
}