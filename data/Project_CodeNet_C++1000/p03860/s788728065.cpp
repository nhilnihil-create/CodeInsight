#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, srt, end) for (long long i = (srt); i < (long long)(end); i++)

int main(){
    string s[3];
    rep(i, 0, 3) cin >> s[i];
    rep(i, 0, 3) cout << s[i][0];
    cout << endl;
    return 0;
}