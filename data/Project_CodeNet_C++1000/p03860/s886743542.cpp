#include "bits/stdc++.h"
using namespace std;
using ll = long long;
using ull = unsigned long long;
#define all(a) (a).begin(), (a).end()


int main() {
    string a, b, c;
    cin >> a >> b >> c;
    string word = {'A', b[0], 'C'};
    cout << word << endl;
}
