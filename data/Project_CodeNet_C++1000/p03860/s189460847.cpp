#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main(){
    string S[3];
    cin >> S[0] >> S[1] >> S[2];
    cout << "A" << S[1].at(0) << "C" << endl;
}