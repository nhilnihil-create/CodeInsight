#include <bits/stdc++.h>
#include <string>
#include <algorithm>
using namespace std; //std::の省略のため
using ll = long long;

int main()
{
  string s[3];
  char ans[3];

  for(int i=0; i<3; i++)
  {
    cin >> s[i];
    ans[i] = s[i][0];
    cout << ans[i];
  }

}
