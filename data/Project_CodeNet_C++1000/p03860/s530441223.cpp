#include <bits/stdc++.h>
#include <vector>

#define rep(i,n) for(int i=0;i<(n);++i)

using namespace std;

int main () {
	string s1, s2, s3; cin >> s1 >> s2 >> s3;
	cout << s1[0] << s2[0] << s3[0] << '\n';

	return 0;

}