#include <bits/stdc++.h>
using namespace std;

int main() {
  

  // ここにプログラムを追記
  string a, s, c;
  cin >> a >> s >> c;
  char A, S, C;
  A = a.at(0);
  S = s.at(0);
  C = c.at(0);
  cout << A << S << C << endl;
}
