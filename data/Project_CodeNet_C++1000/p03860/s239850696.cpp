#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < n; ++i)
using namespace std;
typedef long long ll;
#define MODSIZE 1000000007

int main(){
  char a[10],b[200],c[10];

  scanf("%s %s %s", a, b, c);

  printf("A%cC\n", b[0]);

  return 0;
}
