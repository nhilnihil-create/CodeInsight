#include <iostream>
#include <cmath>
 
using namespace std;
 
int main() {
        int i;
    //??????????????????????????則vector????????????????????測??????vector???????????????????????????????????即????即??????????????????????
    double n, m, s[1000], v;
    
    while(1){
        cin >> n;
        //exit condition
        if(n==0) break;
        
        m=0;
        for(i=0; i<n; i++){
            cin >> s[i];
            m+=s[i];
        }
        m/=n;
        v=0;
        for(i=0; i<n; i++){
            v+=pow(s[i]-m, 2.0);
        }
        
        cout.precision(4); // ????????? 4????????????????????????
        cout.setf(ios::fixed); // ?属???属??即?????????????????即?????????
    
        cout << sqrt(v/n) << endl;
    }
        return 0;
}