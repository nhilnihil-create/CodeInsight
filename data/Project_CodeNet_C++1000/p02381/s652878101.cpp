#include <iostream>
#include<cmath>
#include<stdio.h>
#include <vector>
using namespace std;


int main(){

	double num,score,alpha;

	for (;;){
		cin >> num;
		if (num == 0)break;

		vector<double> students;//??°????????????????????§????????????????¢????

		double m = 0.0;//?????????????????????

		for (int i = 0; i < num; i++){
			cin >> score;
			students.push_back(score);
			m += score;
		}	m /= num;
	

		alpha = 0.0;

		for (int i = 0; i < num; i++){



			alpha += ((students[i] - m)*(students[i] - m));
		}
		alpha /= num;


		if (alpha == 0){ cout <<(double)0.00000000000000<<endl; }
		else{
		cout << printf("%.10f", sqrt(alpha)) << endl;
	}

	}//??°???????????????

	return 0;
}