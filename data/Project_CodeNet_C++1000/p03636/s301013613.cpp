#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for(int i = 0; i < (int)(n); i++)

int main(){
  string x;
  cin >> x;
  cout << x[0] << (x.size() - 2) << x[x.size()-1] << endl; 
}