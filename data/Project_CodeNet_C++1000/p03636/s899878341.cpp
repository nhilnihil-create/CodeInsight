#include<bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define put(i) cout<<fixed<<i<<endl
using namespace std;
using ll = long long;

int main(){
    string s; cin >> s;
    string t;
    
    cout << (s[0]);
    cout << to_string(s.size()-2);
    cout << (s[s.size()-1]) << endl;
}