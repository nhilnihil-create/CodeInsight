#include <bits/stdc++.h>
using namespace std;
int main()
{
    cin.tie(0);
    ios::sync_with_stdio(false);
    string s;
    cin >> s;
    cout << s[0];
    cout << s.length()-2;
    cout << s[s.length()-1] << endl;
}
