#include <iostream>
#include <cstdio>
#include <string>
#include <algorithm>
#include <utility>
#include <cmath>
#include <vector>
#include <queue>
#include <set>
#include <map>
#include <tuple>
#include <numeric>
#include <functional>
using namespace std;
typedef long long ll;
typedef vector<ll> vl;
typedef vector<vector<ll>> vvl;
typedef pair<ll, ll> P;
#define rep(i, n) for(ll i = 0; i < n; i++)
#define exrep(i, a, b) for(ll i = a; i <= b; i++)
#define out(x) cout << x << endl
#define exout(x) printf("%.10f\n", x)
#define chmax(x, y) x = max(x, y)
#define chmin(x, y) x = min(x, y)
#define all(a) a.begin(), a.end()
#define rall(a) a.rbegin(), a.rend()
#define pb push_back
#define re0 return 0
const ll mod = 1000000007;
const ll INF = 1e16;
const ll MAX_N = 100010;

int main() {
   string s;
   cin >> s;

   if(s == "KIHBR") {  // 0
        out("YES");
   }
   else if(s == "KIHBRA") {  // 1
        out("YES");
   }
   else if(s == "KIHBAR") {  // 2
        out("YES");
   }
   else if(s == "KIHBARA") {  // 3
        out("YES");
   }
   else if(s == "KIHABR") {  // 4
        out("YES");
   }
   else if(s == "KIHABRA") {  // 5
        out("YES");
   }
   else if(s == "KIHABAR") {  // 6
        out("YES");
   }
   else if(s == "KIHABARA") {  // 7
        out("YES");
   }
   else if(s == "AKIHBR") {  // 8
        out("YES");
   }
   else if(s == "AKIHBRA") {  // 9
        out("YES");
   }
   else if(s == "AKIHBAR") {  // 10
        out("YES");
   }
   else if(s == "AKIHBARA") {  // 11
        out("YES");
   }
   else if(s == "AKIHABR") {  // 12
        out("YES");
   }
   else if(s == "AKIHABRA") {  // 13
        out("YES");
   }
   else if(s == "AKIHABAR") {  // 14
        out("YES");
   }
   else if(s == "AKIHABARA") {  // 15
        out("YES");
   }
   else {
       out("NO");
   }

    re0;
}