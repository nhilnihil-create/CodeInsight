#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    int a, b;
    cin >> a ;
    //*/
    
    /*
    vector<int> a(3);
    cin >> a.at(0) >> a.at(1) >> a.at(2) ;
    
    sort(a.begin(), a.end());
    */
    
    /*
    if( a % 3 == 0 || b % 3 == 0 || ( a + b ) % 3 == 0  ){
        cout << "Possible" << endl;
    } else {
        cout << "Impossible" << endl;
    }
    */
    
    cout << "ABC" << a << endl;

    return 0; 
}
