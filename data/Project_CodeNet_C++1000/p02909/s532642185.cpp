#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    string a;
    cin >> a ;
    //*/

    /*
    vector<int> vec(3);
    cin >> vec.at(0) >> vec.at(1) >> vec.at(2) ;

    sort(vec.begin(), vec.end());
    //*/
    
    //Sunny, Cloudy, Rainy
    //*
    if( a == "Sunny" ){
        cout << "Cloudy" << endl;
    } else if (a == "Cloudy" ){
        cout << "Rainy" << endl;
    } else if (a == "Rainy" ){
        cout << "Sunny" << endl;
    } else {
        cout << "Snowy" << endl;
    }
    //*/
    
    //cout << ((floor(a / 2) + fmod(a, 2)) / a) << endl;
    
    return 0; 
}
