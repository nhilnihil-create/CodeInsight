#include <bits/stdc++.h>
#include <stdio.h>
#include <math.h>
using namespace std;
using vi = vector<int>;
using vvi = vector<vector<int>>;
using ll = long long int;

int main(){
    int a;
    cin >> a;
    string s; cin >> s;
    if(a >= 3200){
        cout << s;
    }
    else{
        cout << "red";
    }
}