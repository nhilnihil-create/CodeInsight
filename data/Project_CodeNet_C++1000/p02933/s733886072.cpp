#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#include <iostream>
#include <ctype.h>
#include <string>
typedef long long ll;
using namespace std;

int main(){
  int n,m;
  cin>>n;
  string st;
  cin>>st;
  if(n>=3200) cout <<st;
  else cout<<"red";
}