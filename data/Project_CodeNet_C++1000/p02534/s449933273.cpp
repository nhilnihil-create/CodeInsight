#include<bits/stdc++.h>
#define rep(i,n) for(int i=0;i<(n);++i)
using namespace std;
typedef long long ll;
int main() {
	int k;
	cin >> k;
	string ans;
	rep(i, k)
		ans += "ACL";
	cout << ans << endl;
}