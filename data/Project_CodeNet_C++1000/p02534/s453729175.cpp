#include <bits/stdc++.h>
using namespace std;

void solve()
{
    long long k;
    cin >> k;
    for(long long i = 0; i < k; i++)
        cout << "ACL";
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    long long t = 1;
    // cin >> t;
    while (t--)
        solve();
    return 0;
}