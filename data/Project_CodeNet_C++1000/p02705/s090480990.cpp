#include <bits/stdc++.h>

#define F first
#define S second
#define PB push_back
#define PP pop_back
#define sz(x) (int)(x.size())

using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0); cout.tie();

	double PI = 3.141592;
	int r;
	cin >> r;
	cout << fixed << setprecision(8) <<  2 * r * PI << endl;
	return 0;
}
