#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;
using P = pair<int,int>;

int main() {
    int R;
    cin >> R;
    cout << (long double)R * (long double)2 * (long double)(M_PI) << endl;

    return 0;
}
