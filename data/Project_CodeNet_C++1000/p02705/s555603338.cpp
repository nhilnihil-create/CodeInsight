#include <stdio.h>
#include <math.h>

using namespace std;
typedef long long llong;

#define MAX
const llong mod = 1000000000 + 7;

int main()
{
  double R;
  scanf("%lf", &R);

  printf("%lf", 2.0 * M_PI * R);

  return 0;
}