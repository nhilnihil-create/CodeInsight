#include <bits/stdc++.h>
#define rep(i , n) for(int i = 0; i < (int)(n); i++)
using namespace std;
using ll = long long;
int main (){
    int W;
    cin >> W >> W;
    string a , b(W + 2, '#');
    for (cout << b << endl; cin >> a; cout << '#' << a << "#\n");
    cout << b << endl;
}