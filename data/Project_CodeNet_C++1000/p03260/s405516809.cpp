#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main()
{
  int a, b;
  cin >> a >> b;

  int c = a * b;
  if ((a * b) % 2 == 0)
  {
    cout << "No";
  }

  else
  {
    cout << "Yes";
  }

  // cout << (a * b) % 2;
}
