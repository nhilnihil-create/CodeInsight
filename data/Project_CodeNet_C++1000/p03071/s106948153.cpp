#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    int a, b;
    cin >> a >> b ;
    //*/

    /*
    vector<int> vec(3);
    cin >> vec.at(0) >> vec.at(1) >> vec.at(2) ;
    
    sort(vec.begin(), vec.end());
    //*/
    
    //*
    if( a == b ){
        cout << a + b << endl;
    } else {
        cout << max(a, b) * 2 - 1 << endl;
    }
    //*/
    
    //cout << ( a - c )  * ( b - d ) << endl;

    return 0; 
}
