#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (n); i++)
using namespace std;

signed main(void)
{
	int n, k;
	cin >> n >> k;
	if (n >= k * 2 - 1)
	cout << "YES" << endl;
	else
	cout << "NO" << endl;
	return 0;
}
