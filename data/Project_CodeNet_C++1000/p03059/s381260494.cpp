#include "bits/stdc++.h"

using namespace std;

void Main() {
    int A, B, T;
    cin >> A >> B >> T;
    cout << (T / A) * B << endl;
}

int main() {
    std::cout << std::fixed << std::setprecision(15);
    Main();
    return 0;
}
