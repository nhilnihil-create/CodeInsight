#include <bits/stdc++.h>

using namespace std;

int a, b, t;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> a >> b >> t;

    cout << t / a * b;

    return 0;
}
