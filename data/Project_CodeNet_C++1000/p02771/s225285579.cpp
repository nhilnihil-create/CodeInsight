#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    /*
    int a, b;
    cin >> a >> b ;
    //*/

    //*
    vector<int> vec(3);
    cin >> vec.at(0) >> vec.at(1) >> vec.at(2) ;

    sort(vec.begin(), vec.end());
    //*/
    
    //*
    if( vec.at(0) != vec.at(2) && ( vec.at(0) == vec.at(1) || vec.at(2) == vec.at(1) ) ){
        cout << "Yes" << endl;
    } else {
        cout << "No" << endl;
    }
    //*/
    
    //cout << b + 100 * ( 10 - min(a, 10) ) << endl;
    
    //printf("%lf\n", ((a / 2 + a % 2) / a));

    return 0; 
}
