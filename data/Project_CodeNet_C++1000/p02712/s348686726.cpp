#include <stdio.h>

int main (){
	
	int N;
	long long int count=0;
	scanf("%d", &N);
	
	for (int i = 1;i<=N;i++) {
		count; 
		if (i%3==0 || i%5==0) {	
		}
	 else {
			count=count+i ;
		
		}
}
	printf("%lld", count);
	return 0;
	
	
	
}