/*
C++ ???????????????????????????????????¬??????
??????AOJ-ITP1??????????????????
*/
#define _USE_MATH_DEFINES

#include <iostream> // ?¨??????\??????
#include <fstream>  // ??????????????\??????
#include <string>   // ???????????¢???
#include <vector>   // ???????????????
#include <cmath>    // ??°??????
#include <iomanip>  // stream???????????¢???
#include <algorithm>// max??¢??°??????

using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::vector;        // ????´???°????????????????????????
using std::fixed;         // ???????????????????¨???????????????????????????????
using std::setprecision;  // ?????°????°???°?????\???????????°????????????????????¨????????\???

int main() {
	double man = 0;
	double yu2 = 0;
	double yu3 = 0;
	double tye = 0;
	double N;
	double num;
	cin >> N;
	vector<double> A;
	vector<double> B;

	// ??\???
	for (size_t i = 0; i < N; i++)
	{
		cin >> num;
		A.push_back(num);
	}
	for (size_t i = 0; i < N; i++)
	{
		cin >> num;
		B.push_back(num);
	}

	// ???????????????????????¢
	for (size_t i = 0; i < A.size(); i++)
	{
		man += abs(A[i] - B[i]);
	}

	// ???????????????????????¢(2)
	for (size_t i = 0; i < A.size(); i++)
	{
		yu2 += pow(abs(A[i] - B[i]), 2.0);
	}
	yu2 = sqrt(yu2);

	// ???????????????????????¢(3)
	for (size_t i = 0; i < A.size(); i++)
	{
		yu3 += pow(abs(A[i] - B[i]), 3.0);
	}
	yu3 = pow(yu3, 1.0/3.0);

	// ?????§????????§????????¢
	for (size_t i = 0; i < A.size(); i++)
	{
		double tmp = abs(A[i] - B[i]);
		tye = std::max(tye, tmp);
	}
	
	cout << fixed << setprecision(10) << man << endl;
	cout << fixed << setprecision(10) << yu2 << endl;
	cout << fixed << setprecision(10) << yu3 << endl;
	cout << fixed << setprecision(10) << tye << endl;

	return 0;
}