#include <iostream>
#include<cmath>
#include<stdio.h>
#include <vector>
using namespace std;


int main(){

	double n, num,dis,p1,p2,p3,p_infinity=0;
	vector <int> x, y;
	cin >> n;
	p1 = p2 = p3 = 0;

	for (int i = 0; i < n; i++){
		cin >> num;
	x.push_back(num);
    }
	for (int i = 0; i < n; i++){
		cin >> num;
		y.push_back(num);
	}

		for (int i = 0; i < n; i++){
	
		if (x[i] >= y[i])dis = (x[i] - y[i]);
		else dis = (y[i] - x[i]);

		//p=1??????????????????????????¢

		p1 += dis;


		//p=2??????????????????????????¢
		p2 += (dis*dis);

		//p=3?????????????????????????????¢
		p3 += (dis*dis*dis);

		//p=?????????????????????????????¢
		if (p_infinity < dis) { p_infinity = dis; }
	}
		p2 = sqrt(p2);
		p3 = pow(p3,1.0/3.0);
		printf("%.6lf\n", p1);
		printf("%.6lf\n", p2);
		printf("%.6lf\n", p3);
		printf("%.6lf\n", p_infinity);




	return 0;
}