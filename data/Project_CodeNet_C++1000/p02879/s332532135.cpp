#include <bits/stdc++.h>
using namespace std;
double pi = 2 * acos(0.0);
int main(){
        int a, b; cin >> a >> b;
        if (a <= 9 && b <= 9) cout << a*b;
        else cout << -1;
    return 0;
}

