#include <bits/stdc++.h>
using namespace std;
//long long a[200005];
int main ()
{
    ///without test case
    long long int a,b,c,d,t,n,i,j;
    cin >> a>>b;
    if(a>=1 && a<=9   && b>=1 && b<=9 )
    {
        cout << a*b<<"\n";
    }
    else
    {
        cout<< -1<<"\n";
    }
}

