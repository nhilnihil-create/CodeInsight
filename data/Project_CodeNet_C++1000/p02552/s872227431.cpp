#include <bits/stdc++.h>
#define ll long long
#define num_lim 10000
#define MAXN 100000
#define BIGN 1000000000
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define all(v) v.begin(),v.end()
#define sortarr(arr_name,size) sort(arr_name,arr_name + size)
#define test int t;cin >> t;while(t--)
#define take(data_type,variable_name) data_type variable_name;cin >> variable_name;
#define forn(variable_name,start,end,step) for(int variable_name = start;variable_name<end;variable_name+=step)
#define in_arr(data_type,arr,n) data_type arr[n];forn(i,0,n,1) cin >> arr[i]
using namespace std;

int main(){
    fast
    take(int,x);
    x==0?cout << 1:cout << 0;
    cout << '\n';
}