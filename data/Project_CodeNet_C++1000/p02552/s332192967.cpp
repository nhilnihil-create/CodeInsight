#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAXN = 1e5 + 10;

int main() {
    ios::sync_with_stdio(false);
    cout.tie(0), cin.tie(0);
    int x;
    cin >> x;
    if (x == 0) {
        cout << 1 << endl;
    }
    else {
        cout << 0 << endl;
    }
    return 0;
}
