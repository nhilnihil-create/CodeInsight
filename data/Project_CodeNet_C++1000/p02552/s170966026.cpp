#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int x; cin >> x;
    if(x == 0) cout << 1 << '\n';
    else cout << 0 << '\n';
    return 0;
}