#include<iostream>
using namespace std;

int main(){
  int a = (cin >> (a), a),
   b = (cin >> (b), b),
   c = (cin >> (c), c),
   d = (cin >> (d), d);

  if((a*b) >= (c*d))
 cout << a*b;
  else
   cout << c*d;

  //system("pause");
  return 0;
}