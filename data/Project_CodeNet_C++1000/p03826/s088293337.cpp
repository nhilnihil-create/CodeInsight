#include <bits/stdc++.h>
#define REP(i,n) for (int i = 0; i < n; i++) 
using namespace std;

int main() {
  
  int a, b, c, d;
  cin >> a >> b >> c >> d;
 
  int ans = max(a*b,c*d);
  
  cout << ans << endl;

}