#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define repr(i, n) for (int i = (int)(n); i > 0; i++)
const int MOD =1e9+7;
using ll=long long;

int main(){
  int n;
  cin >> n;
  if(n == 1) cout << "Hello" << " " << "World";
  else{
    int a, b;
    cin >> a >> b;
    cout << a+b << endl;
  } 
}