#include <bits/stdc++.h>

using namespace std;
int arr[4];
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int d;
    cin>>d;
    
    if(d==1)
        cout<<"Hello World";
    else
    {
        int a,b;
        cin>>a>>b;
        
        cout<<a+b;
    }

    return 0;
}
