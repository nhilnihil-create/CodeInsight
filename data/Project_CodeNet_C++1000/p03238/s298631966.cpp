#include <bits/stdc++.h>
using namespace std;  

int main(){
    int n, x, y;
    cin >> n;
    if ( n == 1 ) {
        cout << "Hello World\n";
    } else {
        cin >> x >> y;
        cout << x + y << endl;
    }
    
    return (0);
}
