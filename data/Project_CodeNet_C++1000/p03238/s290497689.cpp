#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, srt, end) for (long long i = (srt); i < (long long)(end); i++)

int main(){
    ll N;
    cin >> N;
    if(N == 1){
        cout << "Hello World" << endl;
    }else{
        ll a, b;
        cin >> a >> b;
        cout << a + b << endl;
    }
    return 0;
}