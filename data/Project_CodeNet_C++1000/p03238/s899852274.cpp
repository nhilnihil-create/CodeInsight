#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

int main() {
    int op; cin >> op;
    if (op == 1) {
        cout << "Hello World" << endl;
    }
    if (op == 2) {
        int a, b; cin >> a >> b;
        cout << a + b << endl;
    }
    return 0;
}
