#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
typedef long long ll;
template<typename T>
void print(T x) {
    cout << x << endl;
}
int main(void) {
    int n; cin >> n;
    if(n==1) {
        print("Hello World");
    }else{
        int a, b;
        cin >> a >> b;
        print(a+b);
    }
}
