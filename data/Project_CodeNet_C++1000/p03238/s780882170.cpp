#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
  int X, A, B;
  cin >> X;
  if(X == 1) cout << "Hello World";
  else {
      cin >> A >> B;
      cout << A + B;
  }
   return 0;
}