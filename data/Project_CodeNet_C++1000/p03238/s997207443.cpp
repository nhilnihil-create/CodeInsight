#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin >> n;
    if (n == 1) {
        cout << "Hello World";
    } else if (n == 2) {
        int a, b;
        cin >> a >> b;
        cout << a + b;
    }
}