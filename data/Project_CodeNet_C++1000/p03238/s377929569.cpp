#include<bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define put(i) cout<<fixed<<i<<endl
using namespace std;
using ll = long long;

int main(){
    ll a; cin >> a;
    if(a == 1){
        put("Hello World");
    }else{
      cin >> a;
        ll b; cin >>b;
        put(a + b);
    }
}