#include<bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{
    ll t=1,i,j,k=0,q,p,x,y,n,a,b,d;
    //cin>>t;
    while(t--)
    {
       string s;
       cin>>s;
       cout<<s;
       if(s[s.length()-1]!='s')
        cout<<"s"<<endl;
       else
        cout<<"es"<<endl;
    }

    return 0;
}
