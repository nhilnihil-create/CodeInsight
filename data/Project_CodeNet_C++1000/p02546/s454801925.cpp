#include<bits/stdc++.h>
using  namespace std;

main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long long i,j,k,l,m,n,o,p;
 string s;
 cin>>s;
 l=s.size();
 cout<<s;
 if(s[l-1]=='s')cout<<"es"<<endl;
 else cout<<"s"<<endl;
    return 0;
}
