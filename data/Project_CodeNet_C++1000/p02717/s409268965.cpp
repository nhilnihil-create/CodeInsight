#include <bits/stdc++.h>
#define rep(i,n) for(int i = 0; i < (int)(n); ++i)

using namespace std;
using ll = long long;

ll dp[200005][4];

int main() {
	int x,y,z;
	cin >> x >> y >> z;
	cout << z << ' ' << x << ' ' << y << ' ' << endl;
	return 0;
}
