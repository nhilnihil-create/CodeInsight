#include <bits/stdc++.h>
#define rep(i,n) for(int i=0, i##_len=(n); i<i##_len; ++i)
using namespace std;
using ll = long long;
using d = double;
  
int main(){
  int x,y,z;
  scanf("%d%d%d",&x,&y,&z);
  printf("%d %d %d",z,x,y);
  return 0;
}