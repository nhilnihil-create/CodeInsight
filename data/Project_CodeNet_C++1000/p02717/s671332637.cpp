#include <bits/stdc++.h>
using namespace std;

#define _GLIBCXX_DEBUG

//vector<int> A(M), B(M);
//vector<vector<char>> answer(N, vector<char>(N, '-'));

int main() {
  int x, y, z;
  int a, b, c;
  
  cin >> x >> y >> z;
  a = z;
  b = x;
  c = y;
  cout << a << " " << b << " " << c << endl;
  
  return 0;
}