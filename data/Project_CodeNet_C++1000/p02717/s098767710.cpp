#include <bits/stdc++.h>

using namespace std;

int32_t main() {

    ios_base::sync_with_stdio(false);

    cin.tie(0);

    long long a;

    cin >> a;

    long long b;

    cin >> b;

    long long c;

    cin >> c;

    swap(a,b);

    swap(a,c);

    cout << a << ' ' << b << ' ' << c << '\n';

    return 0;

}

