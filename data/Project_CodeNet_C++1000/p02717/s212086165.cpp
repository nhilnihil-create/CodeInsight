#include <bits/stdc++.h>
using namespace std;

#define ll long long

ll arr[10004];

ll max(ll a, ll b) {
    return a > b ? a : b;
}

int main() {
    int a,b,c;
    cin >> a >> b >> c;
    cout << c <<  " " << a  << " " << b << "\n";
}
