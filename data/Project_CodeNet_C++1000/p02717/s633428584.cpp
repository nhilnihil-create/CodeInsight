#include <bits/stdc++.h>
#define _GLIBCXX_DEBUG
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define repi(i, a, b) for (int i = (int)(a); i < (int)(b); i++)
using namespace std;
typedef long long ll;

int X[5];

int main(){
  rep(i, 3) cin >> X[i];
  cout << X[2] << " " << X[0] << " " << X[1] << endl;;
}
