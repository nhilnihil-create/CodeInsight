#include <bits/stdc++.h>
using namespace std;
double pi = 2 * acos(0.0);
int main(){
        int a, b, c; cin >> a >> b >> c;
        swap(a, b); swap(a, c);
        cout << a << " " << b << " " << c << endl;
    return 0;
}

