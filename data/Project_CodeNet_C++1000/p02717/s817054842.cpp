#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
	int x, y, z; cin >> x >> y >> z;
  	cout << z << ' ' << x << ' ' << y << endl;
  	return 0;
}
