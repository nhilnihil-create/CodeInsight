#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
#define fi first
#define se second
#define pb push_back

const int N = 102;
char s[N];

int main() {
    //ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    scanf("%s", s);
    int n = strlen(s);
    for (int i = 0; i < n; i++) {
        printf("x");
    }
    return 0;
}
