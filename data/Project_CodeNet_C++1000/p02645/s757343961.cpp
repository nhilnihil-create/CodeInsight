#include <bits/stdc++.h>
#define rep(i, n) for(ll i = 0; i < (int)(n); i++)
#define int long long
#define ll long long
using namespace std;
signed main(){
    string s;
    cin>>s;
    cout<<s.at(0)<<s.at(1)<<s.at(2)<<endl;
    return 0;
}