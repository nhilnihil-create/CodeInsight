#include <bits/stdc++.h>

#define int long long

using namespace std;

signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    string str;
    cin >> str;
    if (str == "SUN") cout << "7\n";
    if (str == "MON") cout << "6\n";
    if (str == "TUE") cout << "5\n";
    if (str == "WED") cout << "4\n";
    if (str == "THU") cout << "3\n";
    if (str == "FRI") cout << "2\n";
    if (str == "SAT") cout << "1\n";
}
/*

 */